# CORTEX Dashboard — Vite environment variables
# Copy to .env and adjust as needed.
# All vars must start with VITE_ to be accessible in the browser.

# URL of the cortex-backend server
VITE_API_URL=http://localhost:3001
